# P0：代码实施

**进入条件**：无
**完成标准**：本地验证脚本输出 ✅，改动已提交到 `feat/chinese-fts5-bigram` 分支

---

## 改动顺序

先改 `embeddings.py`（独立，改完即可测），再改 `beam.py`（改动较多，按步骤顺序执行）。

---

## Step 1：embeddings.py（3处）

文件：`mnemosyne/core/embeddings.py`

### 1a. 第39行：`_OPENAI_API_KEY` 读取链

```python
# 原
_OPENAI_API_KEY = os.environ.get("OPENROUTER_API_KEY", os.environ.get("OPENAI_API_KEY", ""))

# 改
_OPENAI_API_KEY = os.environ.get("DASHSCOPE_API_KEY",
                  os.environ.get("OPENROUTER_API_KEY",
                  os.environ.get("OPENAI_API_KEY", "")))
```

### 1b. 第40行：`_OPENAI_BASE_URL` 新增 embedding 专用配置

```python
# 原
_OPENAI_BASE_URL = os.environ.get("OPENROUTER_BASE_URL", "https://openrouter.ai/api/v1")

# 改
_OPENAI_BASE_URL = os.environ.get("MNEMOSYNE_EMBEDDING_BASE_URL",
                   os.environ.get("OPENROUTER_BASE_URL", "https://openrouter.ai/api/v1"))
```

### 1c. `_get_embedding_dim` 维度表（dims 字典，Jina 条目之后）

```python
"text-embedding-v4": 1024,
```

---

## Step 2：beam.py

文件：`mnemosyne/core/beam.py`

### 2a. import re 移至模块顶层（约第40-50行）

在已有 import 块末尾新增：

```python
import re as _re
```

同时删除以下两处函数内部的 `import re as _re`：
- `_fts_search` 函数体内（实际第1439行）
- `_fts_search_working` 函数体内（实际第1476行）

### 2b. 新增 `_cjk_bigram()` 函数（第1435行之前）

```python
def _cjk_bigram(text: str) -> str:
    """CJK-aware bigram tokenizer for FTS5. CJK chars -> unigram + bigram; Latin/digits kept as words."""
    result, buf = [], ''
    for i, c in enumerate(text):
        if '一' <= c <= '鿿' or '㐀' <= c <= '䶿':
            if buf:
                result.extend(_re.findall(r'[a-zA-Z0-9_]+', buf))
                buf = ''
            result.append(c)
            next_c = text[i + 1] if i + 1 < len(text) else ''
            if '一' <= next_c <= '鿿' or '㐀' <= next_c <= '䶿':
                result.append(c + next_c)
        else:
            buf += c
    if buf:
        result.extend(_re.findall(r'[a-zA-Z0-9_]+', buf))
    return ' '.join(result) if result else text
```

### 2c. `_get_connection()` 注册 UDF（第387行之后）

在 `conn.execute("PRAGMA busy_timeout=5000")` 之后、sqlite_vec 加载之前插入：

```python
conn.create_function('cjk_bigram', 1, _cjk_bigram)
```

### 2d. FTS5 trigger 更新（`init_beam()` 第609-650行区域）

逐个替换，**顺序执行**：

```python
# em_ai（原第611行）
cursor.execute("DROP TRIGGER IF EXISTS em_ai")
cursor.execute("""
    CREATE TRIGGER IF NOT EXISTS em_ai AFTER INSERT ON episodic_memory BEGIN
        INSERT INTO fts_episodes(rowid, content) VALUES (new.rowid, cjk_bigram(new.content));
    END
""")

# em_ad（原第616行）— v1.2 修正：old.content 也需 bigram
cursor.execute("DROP TRIGGER IF EXISTS em_ad")
cursor.execute("""
    CREATE TRIGGER IF NOT EXISTS em_ad AFTER DELETE ON episodic_memory BEGIN
        INSERT INTO fts_episodes(fts_episodes, rowid, content) VALUES ('delete', old.rowid, cjk_bigram(old.content));
    END
""")

# em_au（原第621行）
cursor.execute("DROP TRIGGER IF EXISTS em_au")
cursor.execute("""
    CREATE TRIGGER IF NOT EXISTS em_au AFTER UPDATE ON episodic_memory BEGIN
        INSERT INTO fts_episodes(fts_episodes, rowid, content) VALUES ('delete', old.rowid, cjk_bigram(old.content));
        INSERT INTO fts_episodes(rowid, content) VALUES (new.rowid, cjk_bigram(new.content));
    END
""")

# wm_ai（原第629行）
cursor.execute("DROP TRIGGER IF EXISTS wm_ai")
cursor.execute("""
    CREATE TRIGGER IF NOT EXISTS wm_ai AFTER INSERT ON working_memory BEGIN
        INSERT INTO fts_working(id, content) VALUES (new.id, cjk_bigram(new.content));
    END
""")

# wm_ad（第634行）— 不改

# wm_au（原第646行）— 上游已有 DROP，只改 INSERT 内容中的 new.content
# 将原来的 new.content 改为 cjk_bigram(new.content)
```

### 2e. `_fts_search` 查询侧（第1441行之后）

在 `safe_query = ' '.join(safe_query.split())` 之后插入一行：

```python
safe_query = _cjk_bigram(safe_query)
```

### 2f. `_fts_search_working` 查询侧（第1478行之后）

同 2e，在对应位置插入：

```python
safe_query = _cjk_bigram(safe_query)
```

---

## 本地快速验证

在项目根目录运行：

```bash
python -c "
import sys; sys.path.insert(0, '.')
from mnemosyne.core.beam import _cjk_bigram

cases = [
    ('腾讯云VPS', '腾 腾讯 讯 讯云 云 VPS'),
    ('然然',      '然 然然 然'),
    ('Docker容器', 'Docker 容 容器 器'),
    ('5美元',     '5 美 美元 元'),
    ('English',   'English'),
]
for inp, exp in cases:
    got = _cjk_bigram(inp)
    assert got == exp, f'FAIL {inp!r}: got {got!r}, want {exp!r}'
print('✅ _cjk_bigram 验证通过')
"
```

---

## 完成标准

- [ ] `embeddings.py` 3处改动已应用
- [ ] `beam.py` 6处改动已应用（import re、_cjk_bigram、UDF注册、4个trigger更新、2个查询函数）
- [ ] 本地验证脚本输出 `✅ _cjk_bigram 验证通过`
- [ ] `git diff` 确认只改动 `embeddings.py` 和 `beam.py`，无多余改动
- [ ] commit 推送到 `feat/chinese-fts5-bigram` 分支
